 Z语言开发文档 v1.1

 语言设计哲学text
1. 中文友好：关键词全中文，支持Unicode命名
2. 逻辑透明：运行时错误显示中文描述
3. 最小惊喜：语法行为与Python保持高度一致
4. 教学优先：强制缩进，禁止隐式类型转换
---

 语法规范

 1. 基础结构z
> 单行注释
定义 价格 = 88.5        变量声明（自动推断为float）
设 最大重试次数 = 3      常量声明（编译时检查）
2. 控制流
条件分支z
如果 温度 > 30：
    打印("开启空调")
否则 如果 温度 < 10：
    打印("启动暖气")
否则：
    打印("环境适宜")
循环结构z
重复 5 次：             固定次数循环
    发送数据()

当 连接状态 == 活跃：    While循环
    接收消息()

遍历 学生 在 班级名单：   ForEach循环
    统计成绩(学生)
3. 函数系统z
定义函数 加密(明文, 密钥=123)：
    '''
    参数说明：
    明文 - 需要加密的字符串
    密钥 - 默认值为123的整数
    '''
    密文 = 明文 异或 密钥
    返回 密文
4. 数据结构
复合类型z
定义 坐标 = (12.5, 30.4)           元组
定义 学生名单 = "张三", "李四"      列表
定义 配置项 = {                      字典
    "超时时间": 30,
    "重试次数": 3
}
5. 异常处理z
尝试：
    文件 = 打开文件("data.txt")
捕获 异常类型 为 错误：
    记录日志("操作失败：" + 错误信息)
最终：
    关闭文件(文件)
---

 运算符对照表

 Z语言运算符  Python等效  示例 

 ＋  +  3＋5 → 8 
 －    10－2 → 8 
 ×  *  4×3 → 12 
 ÷  /  15÷4 → 3.75 
 余  %  7余3 → 1 
 次方    2次方3 → 8 
 等于  ==  状态等于"成功" 
 并且  and  条件1 并且 条件2 

---

 编译器实现

 转译规则python
 Z语言代码：
定义函数 计算(参数):
    返回 参数 × 2

 转换后Python代码：  
def 计算(参数):
    return 参数 * 2
词法分析流程
原始输入 → 全角符号转换 → 中文标识符替换 → 
正则替换 → 抽象语法树生成 → Python代码输出
---

 扩展开发指南

 添加新语法
1. 修改`zlang.h`中的LEX_RULES：cpp
{ R"(新关键词\s+)", "替换目标" }
2. 更新标准库映射：cpp
{"中文库名", "python_package_name"}
3. 重新编译测试：bash
./build.sh && ./ZL test.z
调试技巧bash
 查看转译中间结果
定义 DEBUG_MODE = 真   编译器将保留临时.py文件
---

 已知限制
1. 不支持lambda表达式
2. 类定义语法暂未实现
3. 异步编程需要直接调用Python库

完整语法规范持续更新中，建议定期同步最新版本 

该文档特点：
1. 分层结构：从设计理念到实现细节渐进展开
2. 双栏对照：关键语法提供中英文等效对照
3. 扩展接口：明确标注二次开发修改点
4. 兼容性说明：标注语法支持边界条件
5. 实践导向**：每个语法点配可运行示例